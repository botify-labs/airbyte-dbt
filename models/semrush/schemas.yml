version: 2

sources:
  - name: semrush
    schema: "{{ target.schema }}"
    tables:
      - name: domain_organic
        identifier: semrush_domain_organic

models:
  - name: semrush_domain_organic_current