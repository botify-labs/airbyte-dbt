version: 2

sources:
  - name: semrush
    schema: "{{ var('connection_schema') }}"
    tables:
      - name: domain_organic
        identifier: semrush_domain_organic

models:
  - name: semrush_domain_organic_current